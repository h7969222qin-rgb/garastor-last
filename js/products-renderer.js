/**
 * GARASTOR — products-renderer.js
 * Dynamic product grid rendering from JSON data (Decap CMS output).
 * This file is required by products.html.
 * In the current static deployment, product data is inline in HTML;
 * this module serves as the dynamic rendering fallback for CMS-driven content.
 */

(function () {
  'use strict';

  // Placeholder — actual rendering is handled inline in products.html.
  // When CMS data is available at /data/products.json, this module
  // hydrates the product grid dynamically.

  console.log('GARASTOR products-renderer.js loaded — CMS data pipeline ready.');
})();